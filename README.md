# Air-Draw
Air Draw is a browser-native, touchless drawing app that lets you paint on your camera feed using only hand gestures — no stylus, no mouse, no screen contact required. It runs entirely in a single HTML file and uses Google MediaPipe Hands (loaded via CDN) to do real-time AI-powered hand landmark detection through your webcam.
